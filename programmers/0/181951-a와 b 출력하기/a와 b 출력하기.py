a, b = map(int, input().strip().split(' '))
# input()으로 한 줄을 문자열로 입력받음
# strip()으로 문자열 앞뒤의 불필요한 공백을 제거함
# split(' ')으로 공백을 기준으로 두 값을 나눔
# map(int, ...)로 나눈 문자열을 각각 정수로 변환함
# 변환된 두 정수를 a와 b에 각각 저장함


print("a =", a)
print("b =", b)
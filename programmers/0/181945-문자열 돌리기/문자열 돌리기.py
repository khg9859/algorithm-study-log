# 문자열은 여러 개의 문자로 이루어져 있다.
# for문을 사용하면 문자열에서 문자를 앞에서부터 한 글자씩 꺼낼 수 있다.
# 예: "abc"를 반복하면 "a", "b", "c"가 차례대로 나온다.
# print()는 기본적으로 출력 후 줄을 바꾸므로,
# 문자를 하나씩 print()하면 각 문자가 세로로 출력된다.
# 문자열을 시계 방향으로 90도 돌린 결과는
# 기존 문자열의 글자들을 위에서 아래로 한 글자씩 출력한 모습이다.

str = input()

for n in str:
    print(n)